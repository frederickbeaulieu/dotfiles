# Uncomment the following line to enable profiling (time taken by each command)
# You also need to uncomment the last line : zprof
# zmodload zsh/zprof

# ~/.zshrc
eval "$(starship init zsh)"
eval "$(zoxide init zsh)"

# Default terminal
export EDITOR=nvim

# Enable VIM mode
bindkey -v

# Aliases
alias n='nvim'
alias cd='z'

# NVM — brew layout first, curl-installer layout as fallback
export NVM_DIR="$HOME/.nvm" # https://github.com/nvm-sh/nvm/issues/539#issuecomment-245791291
[ -d "$NVM_DIR" ] || mkdir -p "$NVM_DIR"
if [ -s "/opt/homebrew/opt/nvm/nvm.sh" ]; then
  . "/opt/homebrew/opt/nvm/nvm.sh" --no-use
elif [ -s "$NVM_DIR/nvm.sh" ]; then
  . "$NVM_DIR/nvm.sh" --no-use
fi
# Only wire nvm's default node into PATH/aliases once one exists
# (fresh machine: brew node serves until `nvm install --default <ver>`)
if command -v nvm >/dev/null 2>&1 && [ "$(nvm version default 2>/dev/null)" != "N/A" ]; then
  export PATH="$(nvm which default | xargs dirname):$PATH"
  alias node='unalias node ; unalias npm ; nvm use default ; node $@'
  alias npm='unalias node ; unalias npm ; nvm use default ; npm $@'
fi

{{ if eq .profile "work" -}}
#GCloud
export PATH=/opt/homebrew/share/google-cloud-sdk/bin:"$PATH"

#Dart
export PATH="$PATH":"$HOME/.pub-cache/bin"

#Android dev tools
export PATH="$HOME/Library/Android/sdk/platform-tools:$PATH"

{{ end -}}
# Golang
export GOPATH="$HOME/go"
export PATH="$PATH:$GOPATH/bin"

# ZSH Plugins
source /opt/homebrew/share/zsh-autosuggestions/zsh-autosuggestions.zsh
source /opt/homebrew/share/zsh-autocomplete/zsh-autocomplete.plugin.zsh

# FZF
source <(fzf --zsh)
export FZF_DEFAULT_OPTS="--preview 'bat --color=always {}'"

# syntax-highlighting must be sourced last
source /opt/homebrew/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

## Yazi
function y() {
  local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
  yazi "$@" --cwd-file="$tmp"
  if cwd="$(command cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
    builtin cd -- "$cwd"
  fi
  rm -f -- "$tmp"
}

# Uncomment the following line to enable profiling (time taken by each command)
# zprof
